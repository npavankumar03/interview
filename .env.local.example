# Database (PostgreSQL)
DATABASE_URL=postgresql://zoommate:zoommate@localhost:5432/zoommate

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Google OAuth (optional — email/password login works without this)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Note: OpenAI, Deepgram, and Stripe keys are configured via the Admin Portal
# Login as admin@zoommate.local (password: admin123) to set them up
